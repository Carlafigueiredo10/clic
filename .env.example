# Copie este arquivo para .env e preencha com sua chave real.
# O .env é ignorado pelo git — nunca será enviado ao GitHub.
# A chave também precisa estar configurada no painel do Netlify (Environment variables) para funcionar em produção.

OPENAI_API_KEY=sk-cole-sua-chave-aqui
